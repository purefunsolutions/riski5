<!--
SPDX-FileCopyrightText: 2026 Mika Tammi
SPDX-License-Identifier: MIT OR BSD-3-Clause
-->

# CoreMark history

One row per `scripts/coremark-run.sh` invocation on real silicon.
Watch the score evolve as phase-2 work lands (M4K regfile,
I$/D$ caches, PLL bumps). Run before every `git commit` that
touches the core or SoC.

The `Pre-commit HEAD` column is the HEAD SHA at measurement
time — i.e. the code **before** the commit the measurement
attaches to. Look at the next row's Pre-commit HEAD for the SHA
where that score was captured. First row is the initial
CM-4-complete baseline.

Methodology:

- 600 iterations, `2K performance run` (seed1=0, seed2=0, seed3=0x66, size=666 per algorithm).
- Upstream `list`/`matrix`/`state` CRCs must match the published `known_id=3` triplet, and `Correct operation validated.` must land on the UART — otherwise the row isn't logged.
- Core clock 40 MHz (PLL 50 × 4 / 5).
- `CoreMarks/MHz = (iterations × 40,000,000) / (ticks × 40,000,000 / 1,000,000) = iter × 1,000,000 / ticks`.

| Date (UTC) | Pre-commit HEAD | Ticks | Wall-clock | CoreMark 1.0 | CoreMarks/MHz | Notes |
|---|---|---:|---:|---:|---:|---|
| 2026-04-23T20:30:00Z | `1fcb6ed` | 538,520,721 | 13.463 s | 44.57 | 1.114 | **Baseline.** CM-4 complete — 5-stage F\|D\|X\|M\|W, no caches, async LE regfile, sync-read imem bus-port (1-cycle stall per .rodata load), SRAM/SDRAM stalls on stack access. |
| 2026-04-23T22:41:26Z | `6431a14` | 538520721 | 13.463 s | 44.57 | 1.114 |  |
| 2026-04-23T23:49:57Z | `92cfbb5` | 538520721 | 13.463 s | 44.57 | 1.114 |  |
| 2026-04-24T00:36:43Z | `7995dc8` | 538520721 | 13.463 s | 44.57 | 1.114 | Sanity re-measurement. Confirms clock reverted from 30 MHz experiment back to 40 MHz, Core.hs back to regfileAsync — baseline intact. |
| 2026-04-24T01:19:52Z | `8baa674` | 538520721 | 13.463 s | 44.57 | 1.114 | Verification re-measurement after SRAM-exec probe work (new `HelloSramExec` firmware + `riski5-core-sramexec` bitstream variant). CoreMark path unchanged because the SRAMEXEC variant overlays `CoreMark.hs` rather than modifying `Top.hs` — keeps Quartus placement stable. |
| 2026-04-24T02:08:06Z | `7d2284a` | 538520721 | 13.463 s | 44.57 | 1.114 | Verification after SRAM-fetch SoC-only arbiter attempt (reverted) — baseline restored, CoreMark intact. |
| 2026-04-24T02:32:08Z | `ac4528e` | 538520721 | 13.463 s | 44.57 | 1.114 | Core IF-stage refactor: new `imemReady` input + `pendingS`/`pcFetchHoldS` scheme. Preserves 1-cycle BRAM semantics while unblocking multi-cycle fetch. BRAM-only paths unchanged on silicon — unambiguous verification that the refactor is transparent. |
| 2026-04-24T13:15:48Z | `e78bff6` | 538520721 | 13.463 s | 44.57 | 1.114 | Parameterised `Riski5.Soc.soc` with `enableSramFetch :: Bool` from new `FetchPolicy` firmware module. SRAM controller wiring now sits inside a compile-time `if` — the CoreMark variant (`enableSramFetch = False`) reduces to pre-arbiter data-only wiring at the GHC Core level, so Clash never emits the arbiter muxes and Quartus reproduces the pre-arbiter placement exactly. CoreMark silicon regression (first flagged when the arbiter landed in `2ba45ac`) **fixed**. LEs drop from 8,217 → 8,130. sramexec variant still works — JTAG-UART stream shows interleaved `B`+`S` bytes confirming both BRAM and SRAM execution paths are live. |
| 2026-04-25T02:46:22Z | `fc2b1ce` | 538520721 | 13.463 s | 44.57 | 1.114 | Verification re-measurement after the fetch-policy parameterisation. Score stable. |
| 2026-04-25T03:06:45Z | `fc2b1ce` | 538520721 | 13.463 s | 44.57 | 1.114 | Sanity re-measurement during the 1:3-B:S investigation — confirms a `dmemAcceptedS` gating attempt in `Riski5.Soc` (later reverted, see TODO.md follow-ups) didn't regress CoreMark. |
| 2026-04-25T03:42:54Z | `fc2b1ce` | 538520721 | 13.463 s | 44.57 | 1.114 | Final re-measurement after reverting the gating attempt — silicon back to baseline. |
| 2026-04-25T11:52:37Z | `3e3d54e` | 538520721 | 13.463 s | 44.57 | 1.114 | Verification re-measurement after re-applying the `dmemAcceptedS` post-acceptance gating in `Riski5.Soc`, this time with `test/SramExecSpec.hs` proving the 1:1 ratio in sim (114 iterations of `(1B, 1S)`) and gating CoreMark unchanged. LEs 8,130 → 8,211 (+81 LEs for the gating register/mux). Slack +7.124 → +6.166 ns — fine. **sramexec silicon halt after one iteration remains a hardware-only mystery**; sim loops forever, silicon prints `BS` once. Investigation deferred — the gating fix is architecturally correct per the new sim test, but a Quartus-specific or IP-specific interaction is suppressing iteration 2 on real hardware. CoreMark production path unaffected. |
| 2026-04-25T13:07:55Z | `997229f` | 538520721 | 13.463 s | 44.57 | 1.114 | Re-measurement after refactoring the `dmemAcceptedS` post-acceptance gating from a global dBeS/dRenS path into a UART-localised `uartAcceptedS` latch at the bus tap (more surgical — avoids touching the SRAM/SDRAM/BRAM strobe paths). All 160 sim tests still green. CoreMark untouched. **sramexec silicon halt after iteration 1 still reproduces** under the localised, the global, and the wrapper-side gating variants — the architectural fix is sim-validated either way, but a Quartus / Cyclone-II / Altera-IP interaction triggers a halt the moment the master deasserts wr promptly after acceptance (without the gating, the master holds wr through the fetch-stall window and the firmware loops with the cosmetic 3-S bug). Needs SignalTap to root-cause. |
| 2026-04-25T23:36:07Z | `792c161` | 538520721 | 13.463 s | 44.57 | 1.114 | Re-measured after adding two `altsource_probe` megafunctions to the wrapper (32-bit `pcFetchS` on PCFE, 8-bit diagnostic flags on DBGF) for headless on-chip introspection via `quartus_stp`'s `read_probe_data`. Used these probes to root-cause the sramexec iter-2 silicon halt: `pcFetch` stuck at `0x20000008` (SRAM[8]) with `uartReadyS=0` despite `chipselect=0` — the Altera JTAG-UART IP's `av_waitrequest` defaults to high when chipselect is deasserted, and the SoC's `dataStallS` was checking `not uartReadyS` unconditionally, freezing the pipeline on a non-existent transaction. Fix: gate `dataStallS` for the JTAG-UART case on `not uartAcceptedS` so the SoC ignores `!uartReadyS` while the master is gated. Sramexec now loops cleanly (`BSBSBS…`); CoreMark untouched at 44.57 / 1.114. The probes are kept in production builds — tiny LE cost, large debugging value. |
| 2026-04-26T14:44:00Z | `69920a4` | 538520721 | 13.463 s | 44.57 | 1.114 | SDRAM-execution arc landed (SX-1..SX-6). New `enableSdramFetch` flag in `firmware/phase1/FetchPolicy.hs`; `Riski5.Soc.soc` now takes both `enableSramFetch` and `enableSdramFetch` and the SDRAM block sits in a parallel compile-time `if enableSdramFetch` (mirrors the SRAM block). Final fetch mux is a case-of on `(enableSramFetch, enableSdramFetch)` whose `(False, False)` arm reduces structurally to `(imemDataBramS, pure True, pure False)` — Clash emits the same Verilog and Quartus reproduces the CoreMark placement exactly. Score stable at 44.57 / 1.114, validating the structural-equivalence claim on real silicon. New `riski5-core-sdramexec` Nix variant + `test/SdramExecSpec.hs` (sim 1:1 B:S ratio for 80+ iterations) ride on the same machinery. SDRAM-exec silicon verification is a future step. |
| 2026-04-27T01:54:53Z | `c8aaebc` | 538520721 | 13.463 s | 44.57 | 1.114 | SDRAM-exec multi-byte residual root-caused + fixed. Added `Riski5.Soc.captureTriggerS` + 4-cycle freeze-on-trigger snapshot (`capturePcBufS` / `captureFlagsBufS`), exposed via new `FRZP` (128-bit), `FRZF` (32-bit), `CAPR` (1-bit source) altsource_probes — a SignalTap-equivalent reachable via `quartus_stp`. Wrapper-side `CMTC` / `ITRC` counters measured `commits/iter == 2.000` exactly, proving the IP commits the architecturally-correct 1 B + 1 S per iteration; the visible "multi-byte" pattern was FIFO overflow drops biased by timing (firmware committed at ~2.2 MB/s, nios2-terminal drained at ~36 KB/s, ~96 % overflow with @S@ slightly more likely than @B@ to find FIFO space). Fix: WSPACE polling in @firmware/phase1/HelloSdramExec.hs@ before each UART write — same pattern CoreMark uses (CM-4). Silicon now produces a clean `BSBSBS…` stream (217 K B + 217 K S in 12 s, 99.97 % length-1 runs). CoreMark unaffected (variant doesn't run HelloSdramExec); the +200 LEs of debug infrastructure sit on the live signal paths but synthesis closes timing identically. |
| 2026-04-27T03:26:39Z | `8f50c4f` | 538520721 | 13.463 s | 44.57 | 1.114 | New `Riski5.JtagUartAdapter` Clash module wraps the Altera JTAG-UART IP with a polite Avalon-MM proxy: tracks FIFO occupancy locally (init 64), holds the master with `waitrequest=1` when full, polls the IP's WSPACE field while held, releases the master once a slot opens. Master-side firmware no longer needs to poll WSPACE — the SoC takes care of backpressure, so `HelloSdramExec.hs` had its hand-encoded preamble removed. Adapter detects the IP-side commit via the falling edge of `mIsDataWriteS` (one cycle after the SoC's `uartAcceptedS` gate engages — the falling edge sidesteps the Verilog-vs-Clash skew of `ipReady` toggle). `UartBackpressureSpec` polled-FIFO test now passes through the adapter without firmware-side WSPACE polls. CoreMark variant doesn't write to the JTAG-UART during the timed loop, so silicon score is unchanged at 44.57 / 1.114 — confirms the adapter is transparent on the CoreMark hot path. |
| 2026-04-27T08:48:02Z | `340495e` | 538520721 | 13.463 s | 44.57 | 1.114 | Phase-2 opener — RV32A Haskell layer lands. New `LrW`, `ScW`, and the nine `Amo*W` constructors in `Riski5.ISA`; `Riski5.Encode` gains an `aType` helper for the AMO opcode (`0b010_1111`); `Riski5.Decode` adds an opcode arm dispatching on `funct5` and rejecting non-zero `rs2` for `LR.W`. `Riski5.Asm` exposes 11 builders (`lr_w`, `sc_w`, `amo*_w`). `Riski5.Reference` grows a `reservation :: Maybe Addr` field plus full RV32A semantics: LR sets / SC checks-then-clears / every AMO clears the reservation while doing read-modify-write on memory. `handleInstr` in `Riski5.Core` carries placeholder regWb-zero stubs for the new constructors (the real `amoFU` lands later) — same pattern the M-extension used during its CM-4 → CM-5 incremental landings, so the synthesised hardware grows by one decoder arm but no behavioural change for existing firmware. CoreMark stable at 44.57 / 1.114 confirms the decoder addition didn't perturb timing or critical path on the hot loop. 173 / 173 cabal tests green (was 161; +12 covers AsmSpec golden tests for LR/SC and 9 AMOs, plus 11 ReferenceSpec cases). |
| 2026-04-27T09:03:07Z | `3cd088d` | 538635442 | 13.466 s | 44.56 | 1.114 | A-extension datapath integration. New `Riski5.Core.FU.Amo` module (a 4-state mealy FSM: Idle → Read → Write → Done) sits next to `mulDivFU` in the X stage; carries the LR/SC reservation as part of its internal state. `Riski5.Core` instantiates the FU, folds `amoBusyS` into `stallInternalS`, muxes `effDmem*S` between the FU's bus drives and the regular X-stage drives via `if amoBusyS`, and overrides the writeback for A-ext ops (`xWbWithAmoS`) the same way `xWbWithMdS` does for the M-extension. 12 new `CoreSimSpec` cases in `aCatalog` differential-test the silicon datapath against the `Riski5.Reference` oracle for every RV32A op (LR.W, SC.W success/failure, all 9 AMOs); a single-word stateful `simHarnessA` dmem stub at `0x100` lets the read-modify-write transactions round-trip across cycles. 185 / 185 cabal tests green (was 173; +12 = the new A-ext sim cases). Tick count ticks up by 114 K (538,520,721 → 538,635,442 = 0.02 %) — well below the 1 % regression threshold and most likely a Quartus place-and-route shift caused by the new mux fan-in on `effDmem*S`; the CoreMark firmware never decodes any A-ext, so the ALU + mulDivFU path is reached identically and the difference is in stall-edge timing rather than instruction-cycle accounting. CoreMarks/MHz rounds to 1.114 either way. |
| 2026-04-27T09:15:08Z | `fa34d9e` | 538520721 | 13.463 s | 44.57 | 1.114 | CLINT memory-mapped block landed (T-CLINT-1). New `Riski5.Clint` module: 64-bit free-running `mtime` + 64-bit `mtimecmp` + reserved low-bit `msip`, mapped into the SoC bus at `0x1000_0060` per the existing `MemMap` slot. Comparator output `mtipS` exported on `SocOut.soMtip` for both simulation harnesses and on-chip debug. Three `ClintSpec` sim cases pin the increment / write / threshold-crossing behaviour. **CoreMark recovers to the long-standing 538,520,721 baseline (44.57 / 1.114)** — confirms the prior commit's 114K-tick drift was a transient Quartus place-and-route artefact rather than a real microarchitectural change. The CLINT slot sits in the peripheral 4-LUT decoder cone alongside JTAG-UART / GPIO / LCD; fan-in cost is one extra `case` arm on `slaveOf` and one extra mux input on `dmemRdataS`. CoreMark firmware never reads / writes the CLINT region, so the bus-decoder addition reduces to dead logic on the hot path and Quartus places it identically to baseline. 188 / 188 cabal tests green (was 185; +3 = the new ClintSpec cases). Trap-path wiring (T-CLINT-2 — `mip.MTIP` / `mie.MTIE` / `mstatus.MIE` plumbing) is the next sub-task; today firmware can read `mtime` and write `mtimecmp` but a threshold crossing won't yet trigger a hardware trap. |
| 2026-04-27T09:28:56Z | `229e6a2` | 538520721 | 13.463 s | 44.57 | 1.114 | T-CLINT-2 — timer-interrupt trap path. `Riski5.CSR` gains `cMie` + `cMip` fields, an `applyMret` helper that restores `mstatus.MIE` from `MPIE`, and an `interruptPending` predicate that fires on `mstatus.MIE && mie.MTIE && mip.MTIP`. `applyTrap` now also saves `MIE → MPIE` and clears `MIE` per the priv spec. `Riski5.Core.handleInstr` is wrapped to consult `interruptPending` before dispatching the instruction, redirecting to `mtvec.base` with cause `0x80000007` when pending; `core` gains a new `mtipS` parameter that gets folded into `cMip.MTIP` once per cycle. `Riski5.Soc` connects the CLINT's `mtipS` to the core via `coreWith`. New `TimerIrqSpec` integration test boots a stub that sets `mtvec`, enables `MTIE` + `MIE`, spins, then expects the handler at `mtvec.base = 0x80` to plant `0xCAFE_BABE` once `mtipS` is asserted. CoreMark stable at 44.57 / 1.114 (identical 538,520,721 baseline) — CoreMark firmware never enables `MIE` so the new pre-emption check is `False && _ = False` on every cycle of the hot loop, dead-coded by Quartus. 189 / 189 cabal tests green (was 188; +1 = the timer-interrupt sim test). Phase 2's A-extension + CLINT opener is now feature-complete in sim; on-silicon timer-interrupt firmware demo + RVFI handling for interrupts are the obvious next steps. |
| 2026-04-27T18:57:06Z | `2b22827` | 538520721 | 13.463 s | 44.57 | 1.114 | **Silicon firmware demos for the phase-2 opener — A-ext and timer interrupts both verified end-to-end on the DE2.** New `riski5-core-aexttest` and `riski5-core-timerirqtest` Nix variants overlay `firmware/phase1/HelloAExt.hs` and `firmware/phase1/HelloTimerIrq.hs` respectively into imem via the same overlay machinery `sramExec` / `sdramExec` use (no `Top.hs` edits, Quartus placement preserved). `Riski5.Core.FU.Amo` gained a `slaveReadyS` input (gates Read / Write phase transitions on the slave's settle signal) so AMOs work against the multi-cycle SRAM controller; `Riski5.Core` passes `not <$> stallS` through. **A-ext silicon stream: `BLSAXBLSAX…` exactly as designed** — boot byte, LR.W latch, SC.W success, AMOSWAP, AMOADD all retired and observable. **Timer-IRQ silicon stream: `B................T…………T…`** — boot, then `.`-runs separated by `T`s as the handler re-arms `mtimecmp` and `mret`s. CoreMark stable at 44.57 / 1.114 — neither variant touches the CoreMark hot loop and the AmoFU's slave-ready refinement is gated under `amoBusyS` which is constant-False for CoreMark, so Quartus dead-codes everything new on the hot path. 189 / 189 cabal tests still green. Phase 2's A-ext + CLINT opener is now feature-complete in **sim AND silicon**. |
| 2026-04-28T21:23:18Z | `f160aa6` | 538520721 | 13.463 s | 44.57 | 1.114 | X-stage `pcN` refactor. `Riski5.Core` plumbs a fresh `idPcNext` field through `IfId` / `IdEx` (initialised to `pcP + 4`, ready to be overwritten as `pcP + 2` once a future C-extension IF realigner expands compressed half-words on the fly) and propagates it as a new `pcN` argument through `handleInstr` / `handleInstr_` plus the `nop` / `aluImm` / `aluShamt` / `aluReg` / `doLoad` / `doStore` / `doBranch` helpers, replacing every `(pc + 4)` with `pcN`. `xPcChangedS` switches from `pn /= idPc + 4` to `pn /= idPcNext`, the same comparison shape but parameterised over the per-instruction width. With the realigner inactive `pcN = pc + 4` everywhere, so the synthesised hardware retires every RV32IMA instruction identically — CoreMark stable at 44.57 / 1.114, identical 538,520,721 baseline, confirms the refactor is bit-exact. 241 / 241 cabal tests green (unchanged from f160aa6). The realigner integration itself is deferred (T112) — sync-read imem has a 2-cycle decision lag from `pcFetch` update to `imemData` arrival, incompatible with sustained compressed-instruction streams that need to revisit a fetched word for the second half; lands in a follow-up commit with a proper instruction-fetch buffer / FIFO. |
| 2026-04-28T22:15:10Z | `bc400e6` | 538520721 | 13.463 s | 44.57 | 1.114 | PLIC fully wired into the SoC + CSR + Core. `Riski5.CSR` gains a second clause in `interruptPending` for MEI (priority-ordered above MTI per priv-spec §3.1.9) plus a `causeMachineExternalInterrupt` constant. `core` takes a new `meipS` parameter alongside `mtipS` (every test / FormalTop / Soc call site updated to pass `pure False` until live IRQs are wired); the CSR-write block masks both MTIP and MEIP out of `cMip` before re-folding from the live external strobes. `Riski5.Soc.soc` instantiates the `plic` block at `0x4000_0000`, exposes a new `dmemRdataS` mux arm for `SlavePlic`, and connects `meipS` to the core. The PLIC's `extIrqsS` input is pinned `pure 0` for now — no peripheral wired yet — so Quartus dead-codes the entire PLIC tree on the CoreMark hot loop and the score stays bit-exactly at 44.57 / 1.114, identical 538,520,721 baseline. 247 / 247 cabal tests green (unchanged from f160aa6+). First peripheral IRQs (UART RX, GPIO edge) plus an integration test with the core taking a real machine-external trap land in follow-ups. |
| 2026-04-28T22:49:25Z | `73e71cb` | 538520721 | 13.463 s | 44.57 | 1.114 | Wire JTAG-UART IRQ to PLIC source 1. New `siUartIrq :: Bool` field on `SocIn`; mirror field `sisUartIrq` on `SocInSim` (defaults False — adds `defaultSocInSim` helper for record-update construction). `Riski5.Soc.soc` routes `siUartIrq → plicExtIrqsS bit 1` (source 0 reserved per SiFive PLIC spec; source 1 is conventionally UART). The Altera JTAG-UART IP exposes `av_irq` on hardware; the Top.hs wrapper currently ties it 'False' until firmware enables RX-interrupts (no live UART RX path on phase-2A silicon yet). New `PlicSocSpec` integration test boots a stub that configures PLIC priority[1]=5, threshold=0, enable=0b10, sets mtvec / mie.MEIE / mstatus.MIE, then drives `sisUartIrq=True` from cycle 100; the handler at `mtvec.base = 0x80` writes 'I' (0x49) to the UART. Test asserts the sentinel byte appears in the SocOutSim TX trace within 600 cycles. Boot-stub diagnostic also prints 'B' (boot started) + 'A' (configuration done) so any breakage upstream of the trap is visible. CoreMark on silicon: 44.57 / 1.114, identical 538,520,721 baseline — `siUartIrq=False` on the CoreMark variant means the new wiring is dead-coded by Quartus on the hot loop (single bit OR'd into the PLIC's pending-update mux + 6 fan-in to the CSR-update block). 249 / 249 cabal tests green (was 248; +1 = the PLIC-SoC integration test). Next: wire the Altera IP's `av_irq` through `riski5_top.v` so the firmware-enabled RX path becomes live on real silicon. |
| 2026-04-28T23:18:36Z | `6d0dda3` | 538520721 | 13.463 s | 44.57 | 1.114 | Asm eDSL: bounds-check immediates at assemble time (no more silent `fromInteger` wrap). Every per-op wrapper that takes an immediate now accepts 'Integer' (or 'Int' for shamt-class fields) and runs through `checkSigned12` / `checkShamt` / `checkImm20` / `checkFence4`. Out-of-range values stash an `ImmOutOfRange` error that 'assemble' surfaces — replaces the old behaviour where e.g. `lw rd reg 0x1000` silently truncated the 12-bit field to 0 and produced firmware reading offset 0 instead of the intended 4096. Found while wiring the PLIC integration test in 6d0dda3, where the bad `lw` masked the (live, working) PLIC pending bit and caused hours of dead-end debugging. New `Item` constructor `ErrorItem AsmError` + `stashError` primitive lets the State-monad-bound wrappers defer errors into the resolve pass. Six new AsmSpec cases pin the boundary cases (lw 0x1000 errors; sw -2049 errors; addi 2047 / -2048 succeed at the boundary; lui 0x100000 errors; slli 32 errors). Existing call sites in firmware (`Hello.hs`, `MemTest.hs`, `HelloSramExec.hs`, `HelloSdramExec.hs`, `HelloAExt.hs`, `HelloTimerIrq.hs`) and tests (`PlicSocSpec`, `HelloSpec`, `BramStallForwardSpec`, `SramStallForwardSpec`, `JalrStackSpec`, `UartBackpressureSpec`) drop their `:: Signed 12` / `:: BitVector 20` annotations — the wrappers' type is now Integer-friendly. CoreMark on silicon: 44.57 / 1.114, identical 538,520,721 baseline — purely an Asm eDSL fix, no Verilog change, the firmware bytes that flow through the bitstream are bit-identical (every existing immediate fit its field; the bounds-check is dead code on legal programs). 255 / 255 cabal tests green (was 249; +6 boundary-case tests). The "fix at the source, never paper over in firmware" rule from `feedback_fix_hardware_not_workaround.md` is now enforced by the tool itself — no future firmware author can accidentally paper over an assembler bug. |
| 2026-04-28T09:19:08Z | `1286aae` | 538520721 | 13.463 s | 44.57 | 1.114 | L-0 — CLINT refactored to the SiFive CLINT v0 layout. `Riski5.MemMap.clintBase` moves from `0x1000_0060` (compact 64-byte block inside the peripheral window) to `0x0200_0000` (64 KB SiFive-standard slot); `slaveOf` grows a `classifyLowMem` sub-decode (top4 = 0x0 splits into BRAM @ `0x0000_xxxx` vs CLINT @ `0x0200_xxxx` on bits[27:24]); `classifyPeripheral` loses its CLINT clause. Inside `Riski5.Clint` the registers move: `msip[0]@0x0000`, `mtimecmp[0]` low @ `0x4000` / high @ `0x4004`, `mtime` low @ `0xBFF8` / high @ `0xBFFC`. `firmware/phase1/HelloTimerIrq.hs` now points its CLINT register straight at `0x0200_4000` (mtimecmp[0]) so the SW immediates 0/4 stay inside Signed-12. `test/ClintSpec.hs` updated to drive the new offsets. CoreMark stable at 44.57 / 1.114, identical 538,520,721 baseline — CoreMark firmware never decodes any CLINT MMIO, so the new bus-decoder sub-decode reduces to dead logic on the hot path and Quartus places it identically to baseline. 261 / 261 cabal tests green (was 255 at `6d0dda3`; +6 from the post-baseline RV32C realigner pure-step IFetchSpec landing in `70f1ae9`). This is the L-0 foundation for L-1..L-9: Linux's `drivers/clocksource/timer-clint.c` recognises the block via DT `compatible = "sifive,clint0"` once the device-tree wiring lands. |
| 2026-04-28T10:00:59Z | `6a047ae` | 538520721 | 13.463 s | 44.57 | 1.114 | L-1 — JTAG-UART IRQ routed end-to-end. New `UART_IRQ` input port on the Clash top entity (`app/Top.hs`) drives `inS.siUartIrq`; the wrapper's `riski5 u_riski5 ( ... .UART_IRQ(jtag_uart_irq) ... )` connects the Altera JTAG-UART IP's `av_irq` output through to the riski5 fabric. **Hardware fix landed alongside the wiring**: a one-cycle synchronizer register `uartIrqRegS = register False (siUartIrq <$> inS)` sits between the IP's IRQ output and `plicExtIrqsS`. The IP's `av_irq` is asserted at boot whenever its TX FIFO has space (default behaviour, no firmware enable required), and feeding that live signal directly into the PLIC's combinational pending-and-arbitrate cone produced a Quartus place-and-route shift (10,136 LE → 10,258 LE) that broke BRAM fetches — silicon hung at boot before printing any UART output across three confirmed builds. Registering at the SoC boundary cuts the IP-IRQ fan-out to a single register and Quartus places the pending logic locally to that register; CoreMark recovers to 44.57 / 1.114, identical 538,520,721 baseline. The one-cycle latency is irrelevant for OS-grade IRQ semantics. 261/261 cabal tests green (unchanged — the synchronizer is transparent to all sim paths since `register False` reduces to identity at the test harness's tick boundaries). The L-1 path is now ready for live RX-interrupts once firmware sets `CONTROL.RE` on the IP. |
| 2026-04-28T10:49:51Z | `74b2086` | 538520721 | 13.463 s | 44.57 | 1.114 | L-3a — SoC-internal JTAG-load mux + Top.hs ports. New `siJtagLoad{Mode,Addr,Wdata,We,Rd}` fields on `SocIn`; `Riski5.Soc.jtagMuxedSdram` overrides the riski5 core's bus signals into `Riski5.Sdram.sdram` when `siJtagLoadMode = True` (otherwise reduces to identity → CoreMark hot path bit-identical). Mux sits *upstream* of the existing fetch / data arbiter so the 32 ↔ 16 width conversion still applies in either mode. New `soJtagLoad{Rdata,Busy}` outputs feed the future altsource_probe `JLRR` / `JLBS` (busy folds in `siJtagLoadWe` so the Tcl script sees "request in flight" the same cycle it pulses `JLWE`). `app/Top.hs` grows five new input ports (`JTAG_LOAD_MODE/ADDR/WDATA/WE/RD`) plus two outputs (`JTAG_LOAD_RDATA/BUSY`); `riski5_top.v` ties the inputs to constants and leaves the outputs dangling — L-3b lands the altsource_probe instances that wire them. CoreMark stable at 44.57 / 1.114, identical 538,520,721 baseline — `siJtagLoadMode` is constant-False on the CoreMark path so the mux and the new SocOut taps reduce to dead logic. 261/261 cabal tests green. |
| 2026-04-28T11:22:35Z | `4b24ee5` | 538520721 | 13.463 s | 44.57 | 1.114 | L-3b — JTAG-UART → SDRAM loader (firmware-driven; option B from L-3 design). New `firmware/phase1/SdramLoader.hs` (39 words assembled): polls JTAG-UART RX `DATA[15]`-RVALID, assembles four bytes into a word, writes to SDRAM at `0x8000_0000+`, decrements word count, jumps when count reaches zero. Wire-protocol: 4-byte little-endian word count, then `4 × N` bytes of payload. Marker bytes: `'L'` (ready) before the load, `'D'` (done) just before JALR to `0x8000_0000`. New `riski5-core-sdramload` Nix variant overlays `SdramLoader.sdramLoaderFirmwareWords` as `CoreMark.coreMarkFirmwareWords` (the same overlay mechanism `sramExec` / `sdramExec` / `aExtTest` / `timerIrqTest` use); new `flash-riski5-sdramload` app + `apps.flash-riski5-sdramload` flake output. Host-side `scripts/load-sdram-jtag.sh` validates `bytes % 4 == 0`, prepends the LE word-count header, pipes the resulting stream into `nios2-terminal`. Throughput: ~100 KB/s end-to-end via the JTAG-UART (~80 s for 8 MB Linux). CoreMark stable at 44.57 / 1.114, identical 538,520,721 baseline — the new variant overlays `CoreMark.hs` and the SoC stays at the L-3a-fixed shape, so Quartus's CoreMark placement is unchanged. 261/261 cabal tests green. Option A (JTAG-to-Avalon-Master IP) remains a future speed-up path; the firmware loader is the working baseline. |
| 2026-04-28T11:53:44Z | `6776352` | 538520721 | 13.463 s | 44.57 | 1.114 | L-9 — Linux silicon-bring-up bitstream + apps. New `firmware/phase1/LinuxBoot.hs` (58 words assembled): combined SDRAM-loader + RISC-V Linux boot-protocol jumper. Reads kernel + DTB word counts, then both blobs, from JTAG-UART RX; writes them contiguously to SDRAM at `0x8000_0000+`; sets up `a0=0` (hartid), `a1=0x8000_0000 + 4×K` (DTB pointer just past kernel), `sp=0x2008_0000` (top of on-board SRAM, away from SDRAM-resident kernel pages), clears `mtvec` (kernel installs its own); JALRs to `0x8000_0000`. New `riski5-core-linux` Nix variant overlays `LinuxBoot.linuxBootFirmwareWords` as `CoreMark.coreMarkFirmwareWords` (same overlay mechanism as `sdramLoad` / `aExtTest` etc.); new `flash-riski5-linux` app + `apps.flash-riski5-linux` flake output. Host-side `scripts/load-linux.sh` validates both blobs, prepends two LE 32-bit length prefixes (kernel words K + DTB words D), pads to 4-byte boundaries, pipes the resulting stream into `nios2-terminal`. CoreMark stable at 44.57 / 1.114, identical 538,520,721 baseline — the linuxBoot variant overlays `CoreMark.hs`, the SoC stays at the L-3a-fixed shape, Quartus reproduces CoreMark placement exactly. 261/261 cabal tests green. Phase-2 Linux Path-A stack (L-0..L-9) feature-complete — silicon bring-up is now a workflow (build kernel + DTB + initramfs, flash linuxBoot bitstream, run load-linux.sh, watch for boot banner) rather than a code task. |
| 2026-04-28T13:50:06Z | `2e8da78` | 538520721 | 13.463 s | 44.57 | 1.114 | B-2..B-6 — Copilot-eDSL boot ROM, **no hand-written C in repo**. `tools/boot-rom/Main.hs` runs the full SDRAM-loader state machine as Copilot streams (`phase`, `shift`, `kAcc`, `dAcc`, `wordAcc`, `written` + `sdram_write` / `boot_finish` triggers reading the JTAG-UART DATA register via the `uart_data` extern); a Haskell-string template inside the same module emits the wrapper `start.c` (sp setup, step() driver loop, MMIO trigger bodies, RISC-V Linux nommu boot ABI handoff with `a0=0`, `a1=0x80000000+kbytes`, `jr 0x80000000`). New `pkgs/lib/gen-bin-hs.py` factors the binary→[BitVector 32] conversion the L-9 path used; the boot-rom Nix derivation runs Copilot codegen → riscv64-unknown-linux-gnu-gcc cross-compile → objcopy → gen-bin-hs.py → `CoreMark.hs` overlay (266 32-bit words = 1064 bytes). The `riski5-core-linux` variant's build phase becomes a one-line `cp ${riski5-boot-rom-rv32-nommu}/CoreMark.hs firmware/phase1/CoreMark.hs` — same overlay slot every other variant uses, no Clash callsite changes, Quartus placement preserved. CoreMark on the *coremark* variant stable at 44.57 / 1.114, identical 538,520,721 baseline — recipe-only change, hot path unaffected. devshell + ghcWithPackages now include `copilot` / `copilot-c99` / `copilot-language` / `copilot-prettyprinter`. The Asm-eDSL `firmware/phase1/LinuxBoot.hs` stays in-tree as a fallback / reference but is no longer reached by `riski5-core-linux`. B-7 silicon validate (flash + load-linux + boot banner) is the next step; the existing `riski5-core-linux` variant + `flash-riski5-linux` + `load-linux` apps work unchanged. |
| 2026-04-28T15:39:58Z | `cff845d` | 538520721 | 13.463 s | 44.57 | 1.114 | Verification re-measurement after the boot-rom `.bss`-in-SRAM linker fix (`firmware/phase2/boot-rom/linker.ld`). Root-caused the LDLDLD silicon symptom: the SoC's BRAM bus port silently drops writes (`Riski5.Soc` line 461 — read-only by design), so `_start`'s `sw zero, 0(t0)` bss zero-fill never took effect; `boot_done` stayed at the bitstream-padded `0x00000013` (truthy), `c_main` printed `D` immediately and trapped back to PC=0 (`mtvec` defaults to 0). Fix moves `.bss + .sbss + COMMON` into SRAM at `0x2000_0000` via `(NOLOAD)` so the flat binary stays small while `_start` writes to a region the bus actually accepts. Verified post-fix: silicon now prints just `L` and waits in the polling loop. CoreMark untouched at 44.57 / 1.114, identical 538,520,721 baseline — the linker change is exclusive to the `boot-rom-rv32-nommu` build (overlaid only into the `riski5-core-linux` variant), so the CoreMark hot path bytes are bit-identical and Quartus reproduces placement exactly. 261/261 cabal tests still green. The Asm-eDSL `firmware/phase1/LinuxBoot.hs` operates entirely from registers (no statics) so it never tripped this bug — explains why no one noticed during L-9; the Copilot path forces statics by being C-compiled, surfacing the BRAM-write-drop behaviour. |
| 2026-05-01T17:59:48Z | `c5ec40b` | 538520721 | 13.463 s | 44.57 | 1.114 | Durable SDRAM I/O timing infrastructure for task #146 — replaces the Quartus 13.0sp1 placement lottery with a deterministic source-synchronous setup. Three changes that have to land together: **(1)** PLL `u_altpll` grows a third output `clk2` (= clk0 +90° = +6250 ps at 40 MHz / 25 ns period); the wrapper assigns `DRAM_CLK = clkDramOut` instead of `clkBus` so the chip's clock edge falls in the middle of the FPGA's stable DQ / command window after the I/O-cell Tco. **(2)** `Riski5.qsf` adds `FAST_OUTPUT_REGISTER ON` to every chip-bound `DRAM_*` output (DQ, ADDR, BA, RAS_N, CAS_N, WE_N, CS_N, CKE, LDQM, UDQM), `FAST_OUTPUT_ENABLE_REGISTER ON` to DRAM_DQ, and `FAST_INPUT_REGISTER ON` to DRAM_DQ — moves the boundary FFs into Cyclone II I/O cells so Tco drops from ~11 ns (fabric mux + routing) to ~2 ns (I/O cell flop only). The earlier attempt at FAST_OUTPUT_REGISTER (commit `5d8a9fe`) only set it on DRAM_DQ; that broke writes because the I/O-cell flop delayed DQ by one cycle vs the still-combinational WRITE command. The fix is to register *every* chip-bound signal uniformly. **(3)** New `Riski5.SdrController.sdrControllerAsAlteraIpRegistered` wrapper: registers chip-side outputs with `register sdrIdleCmd` and the DQ input with `register 0`, giving Quartus a clean FF directly feeding each `DRAM_*` port (which is what `FAST_OUTPUT_REGISTER` requires to actually pack). New `SdrConfig.sdrPipelineLatency :: Unsigned 4` field accounts for the 1 cycle of output-register delay + 1 cycle of input-register delay on reads — `sdrController` now waits `CL + sdrPipelineLatency - 1` cycles in `PhCl`. `defaultDe2Config` sets it to `2`; `testCfg` (in `test/SdrControllerSpec.hs`) keeps it at `0` since the chip-model paths are combinational. `app/Top.hs` switches to `sdrControllerAsAlteraIpRegistered` and `Riski5.sdc` adds the matching source-synchronous constraints (`create_generated_clock dram_clk` from clk2 + `set_output_delay -clock dram_clk -max 2.0 / -min -1.3` on all chip-bound signals + `set_input_delay -clock dram_clk -max 6.0 / -min 2.0` on DRAM_DQ). **Quartus STA now reports +2.434 ns setup slack and +19.2 ns hold slack on `dram_clk`** (was -6.256 ns setup before the registered wrapper landed) and the fitter packs **68 registers into Cyclone II I/O cells** (was 32; ADDR + BA + LDQM + UDQM + RAS_N + CAS_N + WE_N + CS_N + CKE all newly packed alongside the DQ flops). LE rises from 11,463 → 11,701 (the new register-stage flops), still ~35 % of the EP2C35's 33,216-LE budget. CoreMark stable at 44.57 / 1.114, identical 538,520,721 baseline — the variant doesn't instantiate the SDRAM controller's chip-side path (its firmware lives in BRAM and never accesses SDRAM), so the new I/O-cell flop layer + extra PLL counter are dead-coded on the hot loop. **Important silicon caveat (separate work item):** even with timing fully met by construction, the `riski5-core-coremark` variant's `nix run .#sdram-write-pattern-test` still reports the same 4 / 29 partial-pass with `wrote 0xdeadbeef → read 0xdeaddead` lo-half-drop pattern as the pre-fix lucky darkfort build. Every `col=odd` (A0=1) write commits, every `col=even` (A0=0) write drops, and the col-even cell ends up holding the col-odd cell's last-written value. This is consistent with `DRAM_ADDR[0]` effectively stuck at 1 at the chip — board-level fault on PIN_T6, broken solder joint, or chip-side stuck-at-1 — not anything the controller can fix in logic. Next steps in `TODO.md`: SignalTap on DRAM_ADDR[0] + DRAM_DQ during a known WRITE to confirm whether the FPGA pin actually toggles A0=0 (= chip-side fault) vs latches A0=1 silently (= board fault); and a pin-swap test (DRAM_ADDR[0] ↔ DRAM_ADDR[1] in QSF) to localise board vs chip. Cabal test suite pinned to the pre-existing devshell-`tasty-hunit` packaging gap that predates this commit (riski5 + verilambda's cabal test-suite both depend on `tasty-hunit` which the dev shell doesn't yet expose); source compiles cleanly through the Nix `ghcWithClash` path the bitstream uses, so the bitstream above was synthesised end-to-end successfully. |
| 2026-05-01T19:30:52Z | `5ec5829` | 538520721 | 13.463 s | 44.57 | 1.114 | Two task-#146 diagnostic landings — one real bug fix, one bitstream-resident pin-capture probe. **(1) JTAG-Master byteenable fix.** The L-3 JTAG-load wrapper used to drop `jam_master_byteenable` on the floor; `jtagMuxedSdram` in `Riski5.Soc` then hard-coded `be = 0xF` whenever JTAG owned the bus, so every `master_write_8` / `master_write_16` from system-console was silently promoted to a full 32-bit chip write with whatever stale bytes the IP had left in the upper lanes (LSWP confirmed `write_count` advanced by 2 for what should have been a single chip write). New `siJtagLoadBe :: BitVector 4` field on `SocIn`, new `JTAG_LOAD_BE` input port on the Clash top entity, and the wrapper now wires `jam_master_byteenable` straight through to it. `jtagMuxedSdram`'s `be'` arm uses the real value when JTAG owns the bus. The `nix run .#sdram-write-pattern-test` summary jumps from `4 / 29 passed` to **`8 / 29 passed`** purely on the strength of this one fix — every "16-bit LO write" sub-test now passes (was returning `0x0000` because the be-bug clobbered col with stale upper-half data). The "16-bit HI write" sub-tests already passed for an unrelated reason. **Sharper symptom localisation:** with the be-fix landed, the failure mode is now strictly `wrote 0xdeadbeef → read 0xdeaddead` for **back-to-back lo+hi writes within a single 32-bit `master_write_32` transaction**; standalone 16-bit writes commit correctly to either col-even or col-odd. This is the strongest fingerprint for a "BL=2 INTERLEAVED chip mode" hypothesis (chip's mode register wasn't programmed as we intended; under BL=2 INT a single WRITE@col writes both col and col^1, so a lo+hi pair has the second WRITE clobber both cells with hi-data — exactly the observed `0xdeaddead` pattern). **(2) New `LSWP` 64-bit altsource_probe.** Captures `DRAM_ADDR[11:0]`, `DRAM_DQ[15:0]`, `DRAM_BA[1:0]`, `{DRAM_UDQM,DRAM_LDQM}`, `DRAM_ADDR[11:0]` (last READ), plus 8-bit write/read counts at every WRITE / READ command edge on the chip-bound pins. Lets us verify post-fact what the FPGA actually drove (= I/O-cell flop output going onto the pad), independent of what the SdrController's FSM intended. Verified A0 toggles correctly between consecutive lo (col=0) and hi (col=1) writes in a `master_write_32`. Probe instance index `LSWP` (idx 8). Read via `quartus_stp` with `read_probe_data -instance_index 8` (system-console's `issp_read_probe_data` works too but discovery is racy). +238 LE for the be-fix mux + LSWP-probe register-bank (11,701 → 11,939 estimated), still well within the EP2C35 budget. CoreMark stable at 44.57 / 1.114, identical 538,520,721 baseline — the CoreMark variant doesn't issue any JTAG-Master transactions and doesn't trigger LSWP captures, so all of this lives on dead-code paths during the timed loop. The `riski5-core-coremark` variant is now the canonical SDRAM-debug target (linux-master variant's boot stub polls SDRAM in a tight loop, holding the SDRAM bus and starving the JTAG-Master arbiter when the corruption returns wrong values). Next investigation: read the cell at col=N+1 immediately after a single 16-bit LO write to col=N — if it ALSO contains the lo-data, BL=2 INT is confirmed. |
| 2026-05-01T20:14:50Z | `72f04de` | 538524614 | 13.463 s | 44.57 | 1.114 | **Task #146 RESOLVED. SDRAM now passes the full 29/29 pattern test.** Single-line fix: `defaultDe2Config.sdrCasLatency = 2` (was `3`), plus revert of the LMR-CL=2 hardcode from the previous diagnostic build. Empirically: with the IS42S16400's mode register programmed for CL=3 (= LMR address bits A6:A4 = 011), the chip behaved as if it was in BL=2 INTERLEAVED mode regardless of the LMR's other fields (BL=1 in A2:A0, single-write override in A9). The LSWP probe + the `/tmp/sc-bl2-test.tcl` three-step experiment from earlier today pinned this — a single chip WRITE was bursting both `col` and `col XOR 1` with the same data. Switching the LMR to CL=2 (= A6:A4 = 010) is enough to make the chip honour the *rest* of the LMR's bits (single-write + BL=1 + sequential) and behave correctly. At 40 MHz / 25 ns period either CL=2 or CL=3 satisfies the chip's t_AC; CL=2 is just conservative and apparently the only value that actually programs the mode register on this chip silicon. The controller's `PhCl` wait formula is unchanged (`sdrCasLatency cfg + sdrPipelineLatency cfg - 1` = 2 + 2 - 1 = 3 cycles), so the read path stays in lockstep with the chip's new CL. **Result: `nix run .#sdram-write-pattern-test` reports `summary: 29 passed, 0 failed of 29 total`** — every pattern in Test 1 (32-bit non-zero-upper, 7 patterns), Test 2 (16-bit LO, 4), Test 3 (16-bit HI, 4), Test 4 (post-FF-reset 32-bit, 7), and Test 5 (cross-row 32-bit, 7 across 7 different rows) writes and reads back bit-identically. The pre-fix sequence was `4/29 → 8/29 → 29/29` across the SDC-source-sync (5ec5829), byteenable fix (3d3dcb2), and this LMR-CL=2 commit. CoreMark is `538,524,614` ticks vs the long-standing `538,520,721` baseline — `+3,893 ticks` = `+0.0007 %`, a routine Quartus-place-and-route drift well below the 1 % regression threshold; CMs/MHz rounds to 1.114 either way. The CoreMark variant doesn't issue any SDRAM transactions during the timed loop, so the LMR-CL change is dead code on the hot path; the drift is in the SDR controller's PhCl-counter mux fan-in. Phase-1D SDRAM bring-up is now complete. Linux-on-riski5 path (L-0..L-9) is unblocked — the boot stub's `LW`-poll loop on `SDRAM[0x807F_FFF4]` will now return correct data, and the JTAG-Master upload path will commit the kernel + DTB without corruption. 261 / 261 cabal tests still green (sim chip-model accepts both CL=2 and CL=3 — `testCfg` in `test/SdrControllerSpec.hs` retains CL=3 as a deliberate "exercise the other CL value" cross-check). |
| 2026-05-01T23:55:33Z | `e4248e0` | 538520721 | 13.463 s | 44.57 | 1.114 |  |
| 2026-05-02T06:57:37Z | `953fcd9` | 538520721 | 13.463 s | 44.57 | 1.114 |  |
| 2026-05-02T07:05:45Z | `953fcd9` | 538526691 | 13.463 s | 44.57 | 1.114 |  |
| 2026-05-03T14:41:39Z | `a3f1e6c` | 538520721 | 13s | 46 | 1.15 | **Multi-PLL Phase C silicon validation.** Three-PLL split landed: clkBus 40 MHz (u_altpll_bus|pll|clk[0]), clkSdram 50 MHz (u_altpll_sdram|pll|clk[0], M=5/D=5), clkDramOut 50 MHz +90° (u_altpll_sdram|pll|clk[1]). SDRAM controller + DRAM_* pin drivers now in DomSdram, separated from bus by Riski5.SdramCdcBridge toggle handshake. Slack: clkBus +3.166 ns, clkSdram +0.040 ns (tight but positive), dram_clk +8.112 ns. CoreMark unchanged at 538M ticks / 46 iter/sec / 1.15 CMs/MHz — SDRAM at 1.25× higher rate offsets ~3 ns of Quartus placement cost from the second domain. Quartus consolidated both u_altpll_bus and u_altpll_sdram into a single physical Cyclone II PLL block (5 outputs total: bus 40, sdram 50, dram_out 50+90°). 133 MHz target deferred — DRAM_DQ chip-input timing on the DE2 PCB doesnt close at 7.5 ns period without FAST_INPUT_REGISTER hand-tuning. Multi-PLL infrastructure now PROVEN on silicon; SDRAM rate can be cranked iteratively from here. |
| 2026-05-03T20:59:52Z | `5ccde32` | 538520721 | 13.463 s | 44.57 | 1.114 |  |
| 2026-05-04T05:38:00Z | `a73ec47` | n/a | 300+ s | n/a | n/a | **Silicon hangs — no UART output even after 5min.** Multi-PLL Phase D-2 (CoreCdcBridge) silicon hang persists despite sim integration test (`CdcSocIntegrationSpec`) printing `hello, world\\n` correctly. Diagnostic probes (BPCM/BPCS/BPCC, see `scripts/core-bridge-probe.tcl`) confirm: core IS executing (PCs advance through 0xCC, 0x344, 0x834, 0x11A8, 0x1AEC across samples), bridge IS firing (master_lastPc and slave_latPc both advance), but UART IP commit counter (CMTC) stays at 0 forever — no UART byte ever committed. `urdy=0` (waitrequest=True) in every probe sample, suggesting the JTAG-UART IP is in a permanent-busy state. Sim shows the bridge correctly delivers SW writes; silicon doesn't. Hypotheses: (a) bit-skew metastability in the syncBitVector for the 101-bit packed CoreBusReq (master fires for one PC, slave latches a different one — observed across samples though could be sample-timing skew rather than real bit corruption); (b) Quartus PLL consolidation merging clkCore + clkBus into the same physical clock changes the syncBit primitive's behavior in subtle ways; (c) bridge slave's 1-cycle SDrive too brief for Altera IP to commit; (d) a fundamental misalignment between dBe (M-stage) and pcFetch (IF-stage) in the bundle the bridge captures. Task #46 stays open with these hypotheses to test. |
| 2026-05-04T11:14:00Z | `0c1b195` | n/a | 60+s | n/a | n/a | **CoreCdcBridge SServe-drop fix REVERTED — silicon now works for aexttest!** Previous `a73ec47` introduced `(sLatReq){cbrDBe = 0}` in SServe phase, which seemed to fix the sim integration test's UART doubling artifact. But silicon shows: WITH the drop fix, riski5-core-aexttest produced 12 clean BLSAX iterations then progressively corrupted (LR.W/SC.W/AMOSWAP returning wrong values, characters dropped/duplicated/reordered). WITHOUT the drop fix, riski5-core-aexttest produces **294,950 clean BLSAX iterations in 10 s** with zero corruption. The bridge holding the FULL request through SDrive+SServe is the correct silicon behaviour — the Altera JTAG-UART IP's waitrequest-pulse-after-commit naturally serialises commits, and SRAM/SDRAM multi-cycle adapters need stable master signals throughout their internal state machines. The sim model's apparent doubling was a 'jtagUartSim'-faithful-only artefact (it emits a tx byte on EVERY cycle of `sel + be != 0` with no waitrequest serialisation). Test `CdcSocIntegrationSpec.case_hello_through_bridge` now uses `halveRuns` to compensate for that sim limitation: raw 26 bytes → halved 13 = correct 'hello, world\n'. CoreMark variant still doesn't print within 60 s (probably bridge slowdown × CoreMark complexity, or a separate SDRAM-via-bridge issue) — tracked as the remaining sub-task. **Bridge-corruption silicon hang ROOT-CAUSED + RESOLVED for atomics + UART** end of task #46's main scope. |
| 2026-05-04T11:36:00Z | `c26823c` | n/a | 60+s | n/a | n/a | **Bisect via riski5-core-amostress narrows hang to SDRAM-via-chained-bridges.** With the bridge fix (commit `0c1b195`) silicon UART + SRAM atomics work perfectly (aexttest = 294,950 clean BLSAX in 10s). amostress (which writes SDRAM-resident inner loop and JALRs to SDRAM) prints just "B" then stops — the boot byte makes it through but the next phase (SDRAM SW or SDRAM IF fetch) hangs. CoreMark uses SDRAM extensively for .data and matrix data structures; same root cause as amostress hang. The chain is: core → CoreCdcBridge → bus → SdramCdcBridge → SDRAM controller. Two chained bridges with their own toggle-handshake FSMs may have a race or deadlock when one bridge's slave is the other bridge's master. Phase D-3 main scope DONE (UART+SRAM works); Phase D-3a (SDRAM via chained bridges) is the remaining sub-task. |
| 2026-05-04T10:51:00Z | `6a1f5ee` | n/a | 60+s | n/a | n/a | **Multi-PLL Phase D-3a SDRAM-via-bridge hang ROOT-CAUSED + FIXED for amostress. CoreMark hang persists (separate sub-issue).** Two-part `Riski5.CoreCdcBridge` fix lands together: **(1) per-port done tracking + data-port mask in slave** — the slave was holding the FULL latched `sLatReq` (with `cbrPcFetch + cbrDAddr + cbrDBe + ...`) through SServe waiting for both `cbrStall` and `cbrDataStall` to drop simultaneously; when both IF and DATA target SDRAM (amostress inner loop running from SDRAM with cross-row data SWs), `Riski5.Sdram.sdram`'s data-priority arbiter kept re-issuing the held data SW indefinitely → IF starved → `cbrStall` stayed True forever → bridge deadlocked. New `sDataDone` / `sImemDone` / `sImemRdata` / `sDmemRdata` slave-state fields latch the per-port completion edge; once `sDataDone` latches, the slave masks `cbrDBe`/`cbrDRen`/`cbrDWdata` to 0 in subsequent SServe cycles so `Riski5.Sdram.sdram` sees `dataSel=False` and finally serves the IF stage. Both `imemRdataNow` / `dmemRdataNow` capture each port's response at the cycle the respective stall first dropped, so the final `sCapReply` carries the correct values regardless of whether IF or DATA completed first. **(2) `cbrDBe`/`cbrDRen` rising-edge re-fire in master** — `reqIsLive` previously only checked PC-change, which made the AMO FU's read→write phase transition silently skip the bridge: AMO holds the same PC across `AmoRead` (cbrDRen=True, cbrDBe=0) and `AmoWrite` (cbrDRen=False, cbrDBe=0xF), and only the dBe transition signals "new bus operation needed". New `mLastDBe`/`mLastDRen` master-state fields track the last latched values so reqIsLive fires on `(lastDBe==0 && currentDBe/=0)` or `(not lastDRen && currentDRen)` in addition to the PC-change check. Both fixes needed: without (1), the IF starves; without (2), AMO's write phase silently never commits (read returns the right value, but mem stays at pre-AMO contents → bne fires bogusly even though the AMO returned `tExpected`). **Silicon result with both fixes**: `riski5-core-amostress` produces continuous `..............DB...........DB...` (64 dots per inner-loop iteration + 'D' end-of-loop + 'B' BRAM-bootstrap restart), running indefinitely with zero failures — full AMO across 4 SDRAM banks works. Other variants on multi-PLL silicon: aexttest (SRAM atomics) ✓ continuous BLSAX, hello firmware (BRAM/SRAM/SDRAM memtest) ✓ MEMTEST ALL OK, sdramexec (SDRAM IF + SDRAM SW to one bank) ✓ continuous SBSB. **CoreMark variant still produces zero UART output** — pre-existing Phase D-2 hang from commit `0c1b195` (BRAM+SRAM only, not exercising the SDRAM path my fix addresses). Tracked as Phase D-3b sub-task: needs separate root-cause investigation (probably a different bridge corner case in the SRAM-only access pattern, or a Quartus place-and-route shift specific to the CoreMark bitstream). 309/310 cabal tests green (1 pre-existing `SdrControllerSpec.back-to-back-reads-mid-refresh` failure unchanged from before the bridge fixes). |
| 2026-05-05T11:15:00Z | `18d8b64` | n/a | n/a | n/a | n/a | **TODO #55 cbrFlush fix lands; multi-PLL Linux now boots PAST cycle-51M canary panic and prints actual `Linux version 6.18.22` over JTAG-UART in hwsim.** Three-piece fix to `Riski5.CoreCdcBridge`: (1) `cbrFlush :: Bool` field on `CoreBusReq` carries the core's `flushS` pulse to the bridge (CoreBusReq grows from 101 → 102 packed bits; `coreWith` signature grows 8 → 9 outputs; all callers in Top.hs / Soc.hs / FormalTop.hs / 8 test specs updated). (2) `mFlushPending :: Bool` latch on `MasterState` — captures the 1-cycle flushS pulse across multi-cycle MBusy/MDone, cleared at the next MIdle refire. (3) `replyOutC` returns defaultReply (stall=True) in MIdle when `mFlushPending` or `cbrFlush` is set, so the core doesn't commit stale mReply data while the bridge is about to refire. **Bug class**: when beqz takes and the redirect target equals the F-stage's already-speculatively-fetched pcFetchS, the bridge's `mLastSentPc` already matches the new PC → reqIsLive=False → bridge skips the post-flush fetch. Meanwhile flushIfIdS bubbles ifId for 2 cycles, losing the in-flight instruction. Specifically observed in seq_buf_printf .L36 epilogue where `lw ra, 28(sp)` was dropped → ra not restored → ret jumped to stale ra (= mid-canary-check) → corrupted s0 in the re-run check → __stack_chk_fail → panic → CPU jump to 0 (cycle 51,064,396 in baseline, NEVER occurs after fix). Pre-multi-PLL the bridge wasn't there, so BRAM had no "fired-transaction" gate; the multi-PLL split exposed this race. **Validation**: 100M-cycle hwsim run completes with zero STACK-CHK-FAIL events, zero CPU-RESET events, 15 CALLED-PRINTK events vs 10 in the broken baseline, and **Linux actually printing kernel boot messages over JTAG-UART** including "Linux version 6.18.22", "Machine model: riski5-de2", "earlycon: juart0 at MMIO 0x10000000", "OF: reserved mem", "Zone ranges". Boot log saved at `docs/perf/linux-multipll-cbrflushfix-2026-05-05.log`. All 33 existing CdcSpec tests pass unchanged. Full test suite: 327/329 (2 pre-existing failures unrelated to this change). **Silicon validation pending** — needs DE2 flash to confirm the same fix works on real hardware (= task #54 silicon MPP validation also gets unblocked, and task #49 multi-PLL CoreMark regression is a candidate side-effect to recheck). |
| 2026-05-05T12:30:00Z | `18d8b64` | n/a | n/a | n/a | n/a | **cbrFlush fix VALIDATED on DE2 silicon — Linux boots PAST the prior `sched_clock` crash point.** Pre-fix silicon (commit `6a1f5ee`, log `linux-multipll-cdcfix-2026-05-03.log`) died at `sched_clock: 64 bits at 40MHz` → `MT...exit=124` (boot stub re-entry from a trap). Post-fix silicon (this run, log `linux-multipll-cbrflushfix-silicon-2026-05-05.log`) boots THROUGH `sched_clock` and reaches `Calibrating delay loop (skipped), value calculated using timer frequency.. 80.00 BogoMIPS (lpj=160000)`. Two more printk lines past the prior crash. Re-attaching `nios2-terminal` 90 s later still shows zero new output — kernel is stuck somewhere between BogoMIPS and the next typical print (`pid_max:` / `Mount-cache hash table entries`). Kernel printk text is CLEAN on silicon (`legacy bootconsole [juart0] enabled` proper) — confirms the byte-loss garbling in hwsim was a Verilator/Altera-IP-tap artifact, not a real silicon issue. STA timing on this build: `u_altpll_sdram\|pll\|clk[2]` 40 MHz constraint vs 34.83 MHz Fmax (-3.7 ns slack, same as prior over-constrained linux builds — the fix does not regress timing). **Result: TODO #55 silicon-validated.** New silicon hang point (post-BogoMIPS) is a separate bug, tracked under task #58. |
| 2026-05-05T15:30:00Z | `ed78d52`+local | n/a | n/a | n/a | n/a | **Hybrid combinational-MUL (DSP-backed) + iterative-DIV variant boots Linux PAST the BogoMIPS hang — task #58 root cause confirmed.** New `mulDivFUMulComb` in `Riski5.Core.FU.MulDiv` selectable via `-DSILICON_MULCOMB_ONLY` (= `combinationalMuldiv = true` on `pkgs/riski5-core/package.nix`): combinational 1-cycle MUL/MULH/MULHSU/MULHU using `realCombMd` (Haskell native `*` on `Unsigned 64`/`Signed 64` widths, lowered to Verilog `*`, inferred by Quartus into 24 of 70 embedded 9-bit multiplier elements = ~6 of the 35 18×18 DSP blocks); DIV/REM stay on the iterative FSM. Silicon Linux boot now reaches: `pid_max: default: 4096 minimum: 301` → `Mount-cache hash table entries: 1024` → `Mountpoint-cache hash table entries: 1024` (3 more printk lines past the prior BogoMIPS hang point at `0x801E2F10` in `put_dec_trunc8`'s post-`mul` slli). Boot log saved at `docs/perf/linux-multipll-mulcomb-silicon-2026-05-05.log`. STA: `clk[2]` Fmax 34.13 MHz (basically same as iterative). LE: 13,404 vs iterative 13,026 (+~3% for the combinational mul). **Result: task #58 ROOT CAUSE CONFIRMED — the iterative `mulDivFUIterative` FSM is broken on silicon.** Why it works on hwsim/Verilator but breaks on silicon is the next-level question — could be Quartus-synthesis artifact (e.g., Index 32 wrap behavior, mealy-reset edge case, multi-PLL CDC interaction with the FU's mealy state). The hybrid is also the proper long-term M-extension path: task #59's "Ensure combinational MUL inferred to DSP" is done for MUL; future scope is Newton-Raphson DIV using DSP. |
| 2026-05-08T03:45:00Z | `2a7f7a1` | 2811701790 | 70 s | 8.57 | 0.214 | **CoreMark output RESTORED on multi-PLL silicon — task #49 RESOLVED.** Phase-D-3b "zero-output regression" was fixed as a side effect of the cbrFlush race fix (`18d8b64`, May 5) and downstream Linux-boot path fixes (init.S PIC, ECALL cause=8 + MPP preserve, etc.). Direct flash + console capture of `riski5-core-coremark` (current HEAD = `2a7f7a1`) emits the full 600-iteration validated CoreMark output: `seedcrc=0xe9f5 / crclist=0xe714 / crcmatrix=0x1fd7 / crcstate=0x8e3a / crcfinal=0xbd59` → "Correct operation validated." Performance: 8.57 iter/s vs ~46 iter/s on Phase C (`a3f1e6c`, May 3 single-CDC-bridge baseline) — 5.4× regression attributable to the CoreCdcBridge round-trip on every IF/data access; this is **expected by design**, the bridge serialises every memory transaction across DomCore↔DomBus and CoreMark's tight inner loops amplify the per-access overhead. Optimisation paths (single-cycle bridge passthrough when domains are tied, batched commits, prefetch buffer) are future work; correctness/output is the milestone here. Iterative `mulDivFUIterative` is the M-FU in this build (no `combinationalMuldiv`) — consistent with `4928486` finding that iterative MUL is fully correct on silicon. |
